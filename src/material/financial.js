const muiStyles = theme => ({
  paper: {
    width: '100%',
    minWidth: 740,
  },
  textfield: {
    marginLeft: theme.spacing.unit * 3,
  },
  switchform: {
    marginLeft: theme.spacing.unit * 3,
  },
});

export default muiStyles;